---
description: Build or redesign a designer portfolio, running direction, motion, audit and case-study passes in order.
---

Invoke the `hani-build-portfolio` skill and follow it.

If the user named a stage (`direction`, `motion`, `audit`, `cases`), start at that
stage and say which earlier stages you are skipping. Otherwise start from the
brief.

User's request: $ARGUMENTS
